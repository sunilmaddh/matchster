import 'package:flutter/material.dart';
import 'package:matchster/core/widgets/fields/common_card.dart';

class ShowVerifiedProfile extends StatelessWidget {
  const ShowVerifiedProfile({super.key});
  @override
  Widget build(BuildContext context) {
    return CommonCard(isBorder: false, widget: SizedBox());
  }
}
